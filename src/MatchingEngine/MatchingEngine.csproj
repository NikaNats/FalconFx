<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UserSecretsId>dotnet-MatchingEngine-uuid</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Aspire.Confluent.Kafka" />
    <PackageReference Include="BenchmarkDotNet" />
    <PackageReference Include="Grpc.AspNetCore" /> <!-- Update to 10.0 when available -->
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\FalconFX.ServiceDefaults\FalconFX.ServiceDefaults.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Server mode generates the base class we need to implement -->
    <Protobuf Include="..\Protos\trading.proto" GrpcServices="Server" />
  </ItemGroup>
</Project>
